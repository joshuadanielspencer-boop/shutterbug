# Playtest feedback — 2026-07-21 (Joshua)

Captured verbatim in intent, grouped and triaged so a fresh session can pick one
up. Nothing here is done yet. Sizes are my estimate, not a promise.

## Do this first — it affects every playtester

### A. Stale build served to returning players
> "Every time I start the game the subtitles are always still the transparent blue
> until I force a reload."

`vite.config.js` already sets `registerType: "autoUpdate"`, `clientsClaim`, and
`skipWaiting` — so the new service worker installs and takes control, but the
**already-open page keeps the assets it loaded with**. The new build only appears
on the next load. That's why a hard reload always "fixes" it.

**Fix:** listen for `controllerchange` (or vite-plugin-pwa's `onNeedRefresh`) and
reload once, automatically, when a new SW takes over. Small change, high value —
right now every other playtester is potentially reporting bugs against a build
that's days old. **Size: S.**

## Maps

### B. Country maps are zoomed too far out
> "India, Italy, Turkey, China need cropping/zooming significantly… audit all the
> country maps… it barely gives the player any perspective they don't have on the
> continent map."

Confirmed as a systemic thing, not four one-offs: the country view fits the
country's bounding box with a flat `VB_PAD = 0.1` margin
(`src/shutterbug-world.jsx:4594`). A country whose bounding box is dominated by
outlying territory (India with the Andamans, Italy with Lampedusa, China with its
western reach) gets framed to the *box*, not to where the country reads.

**Proposed:** audit all 106 countries by computing, for each, the fraction of the
rendered frame the country's own polygon actually fills; anything under a
threshold gets a per-country crop override in data (rule 1 — a data map, not
inline). I can generate the numbers mechanically and bring you a ranked list
before changing any framing. **Size: M for the audit, M–L for the overrides.**

### C. Specific map crops
- **Canada** — zoom/crop way in. **S**
- **USA** — crop east, north, south; **Hawaii in its own inset box** so it shows
  without distorting the mainland. Inset box is new machinery. **S + M**
- **Oceania** — crop in hard: Australia, New Zealand, and the near islands. Should
  *not* cover the eastern Pacific, Hawaii, or the islands off South America. **S**
- **New Caledonia** — needs a higher-resolution plate. **S–M**

### D. Easter Island / the Chilean islands move to South America
`src/data/locations.js:1233` has `continent: "Oceania"`. One-line change per
island — and it does double duty, because CLAUDE.md flags Chile as the only
country with fewer than three mainland landmarks. Moving Rapa Nui to South
America helps Chile *and* lets Oceania crop in (item C). Check the Oceania layer
still has enough entries afterward. **Size: S.**

### E. "Solomon Is." should read "Solomon Islands"
The abbreviation is the **Natural Earth key**, used in `countries.js`,
`travel.js`, `worldmap.js`, `worldmap-robinson.js`, `culture.js` and
`locations.js`. Do **not** rename the key — add a display-name override and render
through it, the same way the generated geography file separates data from display
name. **Size: S.** (Worth grepping for other NE abbreviations at the same time —
there are likely more.)

### F. At least 3 landmarks visible, always
> "I had an assignment where only 2 landmarks showed up in the USA."

CLAUDE.md says a floor already exists that borrows nearest neighbours so no map
shows fewer than three pins. Either it doesn't cover the easiest difficulty, or it
doesn't cover the USA path. **Needs reproducing first** — the dev hook can force
the case. **Size: S once reproduced.**

### G. Distance legend on country maps — *you asked how hard*
Not crazy: **moderate, and worth doing.** The maps are already projected with
known plate coordinates, so a scale bar is real arithmetic, not a fudge — pick a
round distance, convert to plate units at the map's centre latitude, draw the bar.
Two honest caveats: (1) on an equirectangular plate the scale changes with
latitude, so the bar is only exactly right across the middle of the frame — fine
for a big country, misleading for Canada or Russia unless the bar is drawn at the
centre latitude and labelled as such; (2) rule 3 means **miles first, km in
parentheses**. **Size: M.**

### H. Fly to the exact clicked point — *you asked how hard*
**Easy, and I think you're right that it's better.** `launchFlight(from, to, …)`
already takes arbitrary coordinates; the destination is currently snapped to the
target city. Passing the click's own map coordinates instead is a small change.
The nice property you identified is real: landing where they actually pointed
rewards knowing roughly where the country is, without punishing a miss. Applies to
free-choice modes, not hub-based ones. **Size: S.** Keyboard equivalent needs a
thought — a keyboard user selects a pin, not a point, so they'd keep the snapped
behaviour.

### I. Invert the visited-country tint
> "Countries NOT traveled to are lighter; they become deeper/richer once traveled
> to. But not quite as light as they are currently."

Agreed, and it's the better read — the map fills *in* with colour as you go.
Note rule 4: tint alone can't be the only signal, so the visited state needs to
keep a non-colour marker too. **Size: S.**

## Characters and UI

### J. Pickles — free-floating, larger
> "Similar to Mr O rather than being inside a box inside the Perfect Shot box."
**Size: S–M.**

### K. Mr O about 30% smaller
> "When he pops up he's a bit overwhelming for the screen." **Size: S.**

### L. All travel uses the airplane icon
> "Air travel is always what someone would use until they were at the genuinely
> local level, and we don't really do that in this game."

This retires the overland vehicles added in `d93e157` — the tuk-tuk, camel, cog
railway. Same 4s, same music fades. Mechanically it's a **simplification**
(`launchRide` collapses into `launchFlight`), so **Size: S**.

> **Flagging before I do it:** those twelve transport tokens were built
> deliberately as "the one mechanic that teaches how people actually move around a
> place," and the art exists. Your reasoning is sound and it's your call — I just
> want it to be a decision rather than a deletion. Options: (a) do as you say,
> planes everywhere; (b) planes everywhere, but keep the vehicle as a still
> illustration on the arrival card, so the teaching survives without the animation
> claiming you rode a camel between countries. Say (a) and I'll do (a).

### M. Travel music fades over 1s on country-map arrival only
> "I don't want them overlapping hardly at all." **Size: S.**

## The Long Trip

### N. Camera bag as a fourth rail item
> "Above the field guide… compress so all four fit without scrolling. Run-specific
> info (bag contents, 'fair winds') moves inside a popup on the bag."

Confirmed the scrolling: the pack screen is **855px** tall and the mode picker
**822px** in a 1280×720 window, so "Set off" starts below the fold. This is the
same overflow the keyboard audit found (finding 6). **Size: M.**

## Content

### O. Ancient ruins we don't have — *you asked*

We have **37** under `ruins`, and the obvious headliners are all in — Giza, the
Great Wall, Colosseum, Parthenon, Terracotta Army, Petra, Chichén Itzá, Machu
Picchu, Stonehenge, Meroë, Abu Simbel, Teotihuacán, Tikal, Persepolis, Great
Zimbabwe, Pompeii, Nazca. Angkor Wat, Borobudur, Bagan, Lalibela and Sigiriya are
also in, filed under `temple`/`rock`.

Genuine gaps, checked against the data. **None of these are in the game yet;
every one would still need its fact and photo licence verified before it ships
(rule 2).** Ranked by what I think they'd add:

1. **Mohenjo-daro, Pakistan** — the Indus Valley is a whole civilisation the game
   currently has no entry for. Biggest curricular hole on this list.
2. **Göbekli Tepe, Turkey** — the oldest known monumental site; reframes what
   "ancient" means. (Also gives Turkey a second site, see the crop item.)
3. **Ephesus, Turkey** — the Library of Celsus is one of the most photographable
   ruins anywhere.
4. **Nan Madol, Micronesia** — a stone city built on a reef. Would help the thin
   Oceania layer, which item C otherwise shrinks.
5. **Caral, Peru** — among the oldest cities in the Americas, contemporary with
   Egypt's pyramids; a natural companion to Machu Picchu.
6. **Cahokia Mounds, USA** — the largest pre-Columbian city north of Mexico, and
   almost no American child is taught it.
7. **Ġgantija, Malta** — freestanding stone temples older than the pyramids.
8. **Skara Brae, Scotland** — a Neolithic village, not a monument; a different
   *kind* of ruin to the rest of the list.
9. **Volubilis, Morocco** / **Leptis Magna, Libya** — Roman Africa beyond Tunisia.
10. **Hampi, India** / **Ajanta & Ellora, India** — India has none under `ruins`.

Also absent and defensible to add later: Baalbek (Lebanon), Masada (Israel),
Hattusa (Turkey), Chan Chan (Peru), Aksum (Ethiopia), Choquequirao (Peru).

## Housekeeping

`CLAUDE.md` says "144 places". The file now holds **447 locations across 106
countries**. Correct it next time that file is touched.
